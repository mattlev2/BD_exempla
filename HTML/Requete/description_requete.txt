Ici doit apparaître les fichiers permettant la consultation de la base de données.

A) Requête
  En fonction de la requête (il faut donc des formulaires un peu complexes), on va interroger plusieurs tables: 
- par persName => la table ou c'est l'objet principal (suppose un recherche par nom régularisé / par nom qui apparaît / par surnom)
- par thème => la table où les thèmes sont recensés avec leur localisation précise
- par date 
- par titre?

Le plus dur concernera la recherche de chaînes de caractères. Suppose de savoir comment aller fouiller le XML, et aussi d'avoir des réponses contextuelles (avec un peu de chaînes de caractères avant et après)

B) Traitement
L'idée serait de:

1) Aller chercher le / les fichiers xml dans lesquels est l'information souhaitée: facile (sauf cas de recherche de chaîne de caractères).
2) Fusionner ces fichiers, ou faire en sorte que le fichier de sortie soit un
3) Transformer le fichier en ne faisant apparaître que les exemples qui intéressent (suppose de créer des xsl dynamiques et de les appliquer à chaque requête = très long sur un gros corpus?) en un fichier html lisible: en gros, aller afficher tel, tel, tel segment. 


Quels bouts du texte sont cliquables (avec des liens vers d'autres parties?) => suppose la pré-existence de fichiers déjà tout faits? 

